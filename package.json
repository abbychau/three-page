{
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run copy",
    "copy": "xcopy /s /y assets dist\\assets",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.154.0"
  },
  "devDependencies": {
    "vite": "^4.4.7"
  }
}
